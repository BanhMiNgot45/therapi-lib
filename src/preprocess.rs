pub mod preprocess {
    struct Data {
        url: str;
        data: Vec<str[]>;
    }

    impl Data {
        fn init(url: str) -> Data {
            self.url = url;
            self.data = Vec::new();
        }

        fn read() {
            
        }
    }
}